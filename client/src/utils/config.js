export const SVG_WIDTH = 600;
export const SVG_HEIGHT = 400;

export const SVG_MARGINS = {
    top: 40,
    bottom: 40,
    left: 80,
    right: 40
};

export const MINI_SVG_WIDTH = SVG_WIDTH / 4;
export const MINI_SVG_HEIGHT = SVG_HEIGHT / 4;

export const MINI_SVG_MARGINS = {
    top: SVG_MARGINS.top / 4,
    bottom: SVG_MARGINS.bottom / 4,
    left: SVG_MARGINS.left / 4,
    right: SVG_MARGINS.right / 4
};
